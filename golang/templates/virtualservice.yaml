{{- if .Values.istio.enabled -}}
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: {{ template "golang.name" . }}
  labels:
    app: {{ template "golang.name" . }}
    chart: {{ template "golang.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
    version: {{ .Values.appVersion }}
spec:
  hosts:
  {{- range .Values.istio.hosts }}
    - "{{ . }}"
  {{- end }}
  gateways:
{{- if .Values.istio.gateways }}
  {{- range .Values.istio.gateways }}
    - "{{ . }}"
  {{- end }}
{{- else }}
    - "{{ template "golang.name" . }}-gateway"
{{- end }}
  http:
  - match:
      - uri:
          prefix: {{ .Values.istio.path | default "/" }}
    route:
    - destination:
        port:
          number: {{ .Values.service.port }} # can be omitted if its the only port for reviews
        host: "{{ template "golang.name" . }}.{{ .Release.Namespace }}.svc.cluster.local"
      weight: 80
{{- end }}